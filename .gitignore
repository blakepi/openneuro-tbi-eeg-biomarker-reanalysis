# ---------------------------------------------------------------------------
# Raw / processed EEG data and fetched dataset metadata are NOT redistributed.
# They are obtained directly from OpenNeuro (see data_availability.md).
# ---------------------------------------------------------------------------
data/raw/
data/processed/
data/metadata_cache/
data/
# keep folder placeholders if you add .gitkeep files:
!data/**/.gitkeep

# EEG / neurophysiology binary formats (never commit)
*.set
*.fdt
*.fif
*.edf
*.bdf
*.vhdr
*.eeg
*.vmrk
*.mat
*.xlsx

# Python environment / caches
.venv/
venv/
env/
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Credentials / secrets (never commit)
.env
*.key
*.pem
*token*
*credentials*
.netrc

# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~
.idea/
.vscode/

# Claude / agent local files
.claude/

# Local agent/audit scratch artifacts not intended for the public reviewer release
audit/
journal_submission/pre_*_backup_*/
journal_submission/reconciliation_report_*/
journal_submission/*_AI_screen_deartificialized/
journal_submission/*_AI_screen_deartificialized.zip
journal_submission/post_phase6_current_*_dir.txt
journal_submission/prereg_fidelity_revision_report_*/visual_qa/
outputs/reanalysis_audit_*/
outputs/download_recovery/
